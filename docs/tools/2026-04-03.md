## 2026-04-03

### 技术改进/重构 (Changed/Refactor)
* 重构 `tools/agent/core/context.py`、`tools/agent/commands/build.py` 与 `test/run.py` 的构建目录解析逻辑，将 Python 工具链默认构建产物从 `apps/build_agent` 统一到根目录 `build/build_agent`，并引入 `build/build_tidy` 作为 `clang-tidy` 默认构建目录。
* 重构 `test/framework/runner/service.py`、`test/suites/workout_calculator/env.toml` 与 `test/framework/core/conf/*` 的测试路径装配逻辑，将测试运行产物从 `test/output/workout_calculator/` 统一迁移到 `output/tests/workout_calculator/`，并拆分 `project_apps_root` 与 `project_build_root` 的职责。
* 重构根目录 `pyproject.toml` 的 Ruff 配置，统一以仓库级规则覆盖 `tools/` 与 `test/`，并删除 `test/framework/pyproject.toml` 与 `tools/pyproject.toml` 的局部配置分叉。
* 更新 `tools/config.toml`、`tools/run.py`、`tools/agent/commands/format.py`、`tools/agent/commands/tidy.py` 与 `tools/agent/commands/tidy_fix.py` 的 Python 工具链入口，使 `format`、`tidy`、`tidy-fix`、`tidy-flow`、`tidy-loop` 全部走根目录 `build/` 口径。
* 更新 `test/framework/runner/formatting.py` 与 `docs/note/analyze/clang_format.md`、`docs/note/analyze/clang_tidy.md` 的调用说明，明确当前 `clang-format` 与 `clang-tidy` 的 Python 官方入口分别为 `python tools/run.py format --app workout_calculator` 与 `python tools/run.py tidy --app workout_calculator`。

### 修复 (Fixed)
* 修复 `test/framework/core/conf/loader.py` 与 `tools/agent/commands/build.py` 的 Ruff 问题，补齐 `raise ... from ...`、`zip(..., strict=False)` 与单行返回语句拆分，使 `ruff check tools test` 可通过。
* 修复 `tools/agent/commands/*`、`test/framework/*` 与 `test/generators/*` 的 import 排序与未使用导入问题，完成当前 Python 基线的自动整理。

### 弃用/删除 (Deprecated/Removed)
* 删除 `apps/workout_windows_cli/scripts/sh/refactor/format.sh`、`apps/workout_windows_cli/scripts/sh/build-sh/check_lint.sh`、`apps/workout_windows_cli/scripts/sh/build-sh/fast.sh` 与 `apps/workout_windows_cli/scripts/sh/build-sh/full.sh`，不再保留对旧 shell 包装入口的兼容，统一使用 `python tools/run.py ...` 作为 Python 工具链入口。
